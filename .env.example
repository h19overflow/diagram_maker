# AWS Configuration
# These values are typically set via Terraform or AWS IAM roles
# For local development, you may need to set these manually
DEFAULT_AWS_REGION = ap-southeast-2
AWS_ACCOUNT_ID = <your-aws-account-id>
AWS_ACCESS_KEY_ID = <your-aws-access-key-id>
AWS_SECRET_ACCESS_KEY = <your-aws-secret-access-key>

# S3 Configuration
S3_PREFIX = <your-aws-account-id>
S3_BUCKET_NAME = <your-s3-bucket-name>
S3_PRESIGNED_URL_EXPIRATION = 3600

# Bedrock LLM Configuration
BEDROCK_MODEL_ID = amazon.nova-lite-v1:0
BEDROCK_TEMPERATURE = 0.1
BEDROCK_MAX_TOKENS = 5000

# Embedding Model Configuration
EMBEDDING_MODEL_NAME = intfloat/multilingual-e5-large
EMBEDDING_DEVICE = cuda
EMBEDDING_BATCH_SIZE = 512
EMBEDDING_DIMENSION = 1024

# Text Chunking Configuration
CHUNK_SIZE = 512
CHUNK_OVERLAP = 50

# Retrieval Configuration
BM25_TOP_K = 5
VECTOR_TOP_K = 5
BM25_WEIGHT = 0.7
VECTOR_WEIGHT = 0.3
HYBRID_TOP_K = 10

# API Configuration (optional - defaults are in api_config.py)
# API_HOST = 0.0.0.0
# API_PORT = 8000
# API_CORS_ORIGINS = ["*"]
# STREAMING_MODE = sse
# API_RATE_LIMIT = 100
# LOG_LEVEL = INFO
